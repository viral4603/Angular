import { PhoneMaskingDirective } from './phone-masking.directive';

describe('PhoneMaskingDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneMaskingDirective();
    expect(directive).toBeTruthy();
  });
});
